public class main1 {

    public static void main(String[] args) {
        // download xml and json files and put them in /resources (manually)

        // Parse xml into emissions objects  (predicted)  (print the count of objects ~245)
        // dont do anythign with them yet, disregard


        // Parse JSON into Emissions objects (make this) (actual values) (print the count of objects)

        // Change JSON to add values to an existing emission if the category already exists

        // Now print all emissions objects with both predicted and actual values
    }
}
